<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ownerseye.ownerseye.domain.auth.persistence.mapper.UserMapper">

    <insert id="save">
        INSERT INTO users (email, password, name, created_at)
        VALUES (#{email}, #{password}, #{name}, SYSTIMESTAMP)
    </insert>

    <select id="findByEmail" resultType="com.ownerseye.ownerseye.domain.auth.persistence.entity.UserEntity">
        SELECT user_id, email, password, name, created_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM users
        WHERE email = #{email}
    </select>

</mapper>
